// Jackson Coxson

#include <fstream>

struct LetterEntry {
    char entry;
    int  number; // this is a u32, so hopefully big enough
};

// returns false on errors
bool openFile(std::fstream& input_stream) {
    return true;
}

void count() {
    return;
}

int main() {

    return 0;
}
